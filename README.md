# HTML to PDF conversion using Handlebars and Puppeteer

* Clone the repo
* `npm install`
* `npm start`
* Open `http://localhost:3000/api/html` to develop in html.
* Open `http://localhost:3000/api/pdf` to view rendered PDF.
